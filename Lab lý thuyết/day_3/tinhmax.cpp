#include<stdio.h>
int main (){
    // int a,b,c,d,e,f,s;
    // int maxab,maxcd,maxef;
    // printf("Nhap vao 6 so: "); scanf("%d%d%d%d%d%d",&a,&b,&c,&d,&e,&f);
    // if (a>b) maxab = a; else maxab=b;
    // if (c>d) maxcd = c; else maxcd=d;
    // if (e>f) maxef = e; else maxef=f;
    // s=maxab + maxef + maxcd;
    // printf("Tong la %d",s);
    return 0;
    printf("%d",main);
}